import { Observable } from 'rxjs';
import { AssetEvent } from './files-asset.machine';
import { Asset } from './types';
export declare function manifestParser(root: string): (context: Asset) => Observable<AssetEvent>;
