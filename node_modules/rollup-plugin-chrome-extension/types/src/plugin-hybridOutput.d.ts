import { OutputBundle, Plugin, PluginContext, RollupOptions } from 'rollup';
import { CrxPlugin } from './types';
/** Transforms the pure ESM output bundle into a hybrid ESM/IIFE bundle */
export declare const hybridFormat: () => CrxPlugin;
/** This is really fast b/c we don't use any plugins, and we use the previous bundle as the filesystem */
export declare function regenerateBundle(this: PluginContext, { context, input, output, onwarn }: RollupOptions, bundle: OutputBundle): Promise<OutputBundle>;
export declare function resolveFromBundle(bundle: OutputBundle): Plugin;
