import { BaseAsset } from './types';
export declare function categorizeInput(input: import("rollup").InputOption | undefined, filter: Record<'HTML' | 'MANIFEST', (id: string) => boolean>): {
    crxFiles: BaseAsset[];
    finalInput: string[] | {
        [entryAlias: string]: string;
    };
};
