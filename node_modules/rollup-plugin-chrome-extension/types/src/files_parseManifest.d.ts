/// <reference types="chrome" />
import { AssetType, Manifest, ScriptType } from './types';
/**
 * Returns filenames and match patterns relative
 * to the manifest, which must be in the root folder.
 */
export declare function parseManifest(manifest: Manifest): Record<ScriptType | Exclude<AssetType, 'MANIFEST'>, string[]>;
export declare function deriveFilesMV3(manifest: chrome.runtime.ManifestV3): {
    BACKGROUND: string[];
    CONTENT: string[];
    MODULE: string[];
    CSS: string[];
    HTML: string[];
    IMAGE: string[];
    RAW: string[];
    JSON: string[];
};
export declare function deriveFilesMV2(manifest: chrome.runtime.ManifestV2): {
    BACKGROUND: string[];
    CONTENT: string[];
    MODULE: string[];
    CSS: string[];
    HTML: string[];
    IMAGE: string[];
    RAW: string[];
    JSON: string[];
};
